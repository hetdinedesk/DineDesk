{
  "steps": [
    {
      "name": "install",
      "command": "npm install"
    },
    {
      "name": "build",
      "command": "cd packages/api && npx prisma generate"
    }
  ],
  "deploy": {
    "startCommand": "cd packages/api && npx prisma db push --accept-data-loss --skip-generate && node src/index.js"
  }
}
