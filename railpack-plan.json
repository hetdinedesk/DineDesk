{
  "steps": [
    {
      "name": "install",
      "command": "npm install"
    },
    {
      "name": "build",
      "command": "cd packages/api && npx prisma generate && npx prisma db push --accept-data-loss"
    }
  ],
  "deploy": {
    "startCommand": "cd packages/api && npx prisma db push --accept-data-loss && node src/index.js"
  }
}
