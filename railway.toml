[build]
builder = "dockerfile"
dockerfilePath = "packages/api/Dockerfile"

[deploy]
startCommand = "cd packages/api && echo 'Running migrations...' && npx prisma migrate deploy && echo 'Migrations complete' && node src/scripts/ensureTimezoneColumn.js && node src/index.js"
