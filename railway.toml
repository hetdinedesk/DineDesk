[build]
builder = "nixpacks"
buildCommand = "cd packages/api && npx prisma generate && npx prisma db push --accept-data-loss"

[deploy]
startCommand = "cd packages/api && npx prisma db push --accept-data-loss && node src/index.js"
